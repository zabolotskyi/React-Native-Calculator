import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    input: {
        flex: 1,
        justifyContent: 'center',
        textAlign: 'right',
        marginTop: 50,
        marginBottom: 25,
        borderWidth: 3,
        borderRadius: 5,
        borderColor: 'green',
        color: 'black',
        fontSize: 20
    }
});