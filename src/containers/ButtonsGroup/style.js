import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    buttonRow: {
        flex: 1,
        flexDirection: 'row',
        justifyContent: 'center',
        alignItems: 'center',
        marginBottom: 15
    }
});