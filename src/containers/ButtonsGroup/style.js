import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        flex: 1
    },
    buttonsRow: {
        flex: 1,
        justifyContent: 'space-around',
        flexDirection: 'row'
    }
});